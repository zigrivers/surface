description = "Run scaffold git-workflow through the Scaffold runner workflow."
prompt = """
Use the Scaffold runner workflow already loaded from GEMINI.md.

Step description: Configure git workflow with branching, PRs, CI, and worktree scripts for parallel agents
Purpose: Sets up your branching strategy, commit message format, PR workflow, CI pipeline with lint and test jobs, and worktree scripts so multiple AI agents can work in parallel without conflicts.

User request: scaffold git-workflow
"""
