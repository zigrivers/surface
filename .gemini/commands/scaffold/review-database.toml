description = "Run scaffold review-database through the Scaffold runner workflow."
prompt = """
Use the Scaffold runner workflow already loaded from GEMINI.md.

Step description: Review database schema for correctness and completeness
Purpose: Verifies every domain entity has a table, constraints enforce business rules at the database level, and indexes cover all query patterns from the API contracts.

User request: scaffold review-database
"""
