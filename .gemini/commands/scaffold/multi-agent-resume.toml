description = "Run scaffold multi-agent-resume through the Scaffold runner workflow."
prompt = """
Use the Scaffold runner workflow already loaded from GEMINI.md.

Step description: Resume multi-agent work after a break
Purpose: Verifies the worktree, syncs with main, reconciles completed tasks, and resumes the agent's TDD loop from the previous session.

User request: scaffold multi-agent-resume
"""
