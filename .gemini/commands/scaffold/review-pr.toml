description = "Run scaffold review-pr through the Scaffold runner workflow."
prompt = """
Use the Scaffold runner workflow already loaded from GEMINI.md.

Step description: Run all configured code review channels on a PR (Codex CLI, Gemini CLI, Claude CLI)
Purpose: Run the four built-in CLI review channels (Codex, Gemini, Claude, Grok) on a

User request: scaffold review-pr
"""
