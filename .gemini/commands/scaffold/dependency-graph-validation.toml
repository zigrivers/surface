description = "Run scaffold dependency-graph-validation through the Scaffold runner workflow."
prompt = """
Use the Scaffold runner workflow already loaded from GEMINI.md.

Step description: Verify task dependency graphs are acyclic, complete, correctly ordered
Purpose: Verifies the task dependency graph has no cycles (which would deadlock agents), no orphaned tasks, and no chains deeper than three sequential dependencies.

User request: scaffold dependency-graph-validation
"""
