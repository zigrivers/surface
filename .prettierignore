# Build output & deps
dist/
node_modules/
.turbo/
coverage/

# Tooling / generated state (not ours to format)
.scaffold/
.beads/
.dolt/
.claude/
pnpm-lock.yaml

# Prettier cannot infer a parser for this checked-in env template extension.
.env.example

# Prose & authored content — preserve hand-formatting (tables, HTML comments, YAML overlays)
docs/
content/
fixtures/
*.md
